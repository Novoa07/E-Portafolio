---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Proyectos - Mi Portafolio">
  <section class="proyectos">
    <div class="container">
      <h1 class="page-title">Proyectos</h1>
      <p class="page-subtitle">
        Trabajos y aplicaciones desarrolladas durante el curso
      </p>

      <div class="proyectos-grid">
        <div class="proyecto-card">
          <div class="proyecto-image">
            <div class="image-placeholder">
              <svg xmlns="http://www.w3.org/2000/svg" width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"></path>
                <polyline points="3.27 6.96 12 12.01 20.73 6.96"></polyline>
                <line x1="12" y1="22.08" x2="12" y2="12"></line>
              </svg>
            </div>
          </div>
          <div class="proyecto-content">
            <h3>Proyecto en Desarrollo</h3>
            <p>Los proyectos se irán agregando a medida que avance el semestre.</p>
            <div class="proyecto-tags">
              <span class="tag">SQL</span>
              <span class="tag">Bases de Datos</span>
            </div>
          </div>
        </div>
      </div>

      <div class="empty-state-section">
        <p>🚀 Esta sección se llenará con tus proyectos del curso</p>
      </div>
    </div>
  </section>
</Layout>

<style>
  .proyectos {
    padding: 4rem 0;
    min-height: calc(100vh - 200px);
  }

  .page-title {
    font-size: 3rem;
    margin-bottom: 0.5rem;
    text-align: center;
    background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .page-subtitle {
    text-align: center;
    color: var(--text-secondary);
    font-size: 1.2rem;
    margin-bottom: 3rem;
  }

  .proyectos-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .proyecto-card {
    background: var(--bg-card);
    border-radius: 12px;
    border: 1px solid var(--border);
    overflow: hidden;
    transition: transform 0.3s, border-color 0.3s;
  }

  .proyecto-card:hover {
    transform: translateY(-5px);
    border-color: var(--accent);
  }

  .proyecto-image {
    background: linear-gradient(135deg, var(--bg-secondary) 0%, var(--bg-card) 100%);
    height: 200px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-bottom: 1px solid var(--border);
  }

  .image-placeholder {
    color: var(--accent);
    opacity: 0.5;
  }

  .proyecto-content {
    padding: 2rem;
  }

  .proyecto-content h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
  }

  .proyecto-content p {
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .proyecto-tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .tag {
    background: var(--bg-secondary);
    color: var(--accent);
    padding: 0.375rem 0.75rem;
    border-radius: 6px;
    font-size: 0.875rem;
    border: 1px solid var(--border);
  }

  .empty-state-section {
    background: var(--bg-card);
    padding: 3rem;
    border-radius: 12px;
    text-align: center;
    border: 1px solid var(--border);
  }

  .empty-state-section p {
    color: var(--text-secondary);
    font-size: 1.2rem;
  }

  @media (max-width: 768px) {
    .page-title {
      font-size: 2rem;
    }

    .proyectos-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
